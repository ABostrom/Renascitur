from setuptools import setup, find_packages

setup(
    name="renasci",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.9",
    description="A dynasty-driven narrative simulation engine for the world of Renascitur.",
    author="Aaron Bostrom",
)
